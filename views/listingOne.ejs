<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>One Listing</title>
</head>

<body>
    <h1>One Listing</h1>

    <table class="table">
        <tr>
            <th>Image</th>
            <th>Name</th>
            <th>Bedrooms</th>
            <th>Country</th>
            <th>Summary</th>
            <th>Desc</th>
        </tr>
      
            <tr>
                <td>
                    <div class="container">
                        <img src="<%= listing.images.picture_url %>" alt="">
                    </div>
                </td>
                <td><%= listing.name %></td>
                <td><%= listing.bedrooms %></td>
                <td><%= listing.address.country_code %></td>
                <td><%= listing.summary %></td>
                <td><%= listing.description %></td>
            </tr>
    </table>

    <!-- loops through an array of listings for reviews, showing each listing -->
    <h2>Reviews</h2>
    <ul>
        <% listing.reviews.forEach((listing)=>{ %>
            <li> 
                <h6><strong><%= listing.date %></strong></h6>
                <br> 
                <h4><%= listing.reviewer_name %></h4>
                <br>
                <p><%= listing.comments %></p>
            </li>
        <%  })%>
    </ul>
</body>
</html>